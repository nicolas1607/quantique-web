{% for website in company.websites %}
<div id="modal-delete-website{{website.id}}" class="mdl-delete">
    <div class="mdl-header">
        <a class="mdl-close action-link">✖</a>
    </div>
    <div class="mdl-content">
        <p>Êtes vous sûr de vouloir supprimer l'abonnement suivant : <strong>{{website.name}}</strong> ?</p>
        <div class="cont-flex-space mdl-btn-del">
            <a class="btn-type btn-form btn-alert btn-danger"
                href="{{path('delete_website', {'website': website.id})}}">Supprimer</a>
            <a class="mdl-close-btn btn-type btn-form btn-cancel">Annuler</a>
        </div>
    </div>
</div>
{% endfor %}