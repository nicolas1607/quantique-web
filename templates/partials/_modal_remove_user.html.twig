{% for user in company.users %}
<div id="modal-remove-user{{user.id}}" class="mdl-remove">
    <div class="mdl-header">
        <a class="mdl-close action-link">✖</a>
    </div>
    <div class="mdl-content">
        <p>Êtes vous sûr de vouloir supprimer le client suivant de {{company.name}} : <strong>{{user.firstname}} {{user.lastname}}</strong>
            ?</p>
        <div class="cont-flex-space">
            <a class="btn-type btn-form btn-alert btn-danger"
                href="{{path('remove_user', {'company': company.id, 'user': user.id})}}">Supprimer</a>
            <a class="mdl-close-btn btn-type btn-form btn-cancel">Annuler</a>
        </div>
    </div>
</div>
{% endfor %}