{% for website in company.websites %}
{% for contract in website.contracts %}
<div id="modal-contract{{contract.id}}" class="mdl-delete">
    <div class="mdl-header">
        <a class="mdl-close action-link">✖</a>
    </div>
    <div class="mdl-content">
        <p>Êtes vous sûr de vouloir supprimer le contrat suivant : <br> <strong>{{website.name}} -
                {{contract.type.name}}</strong> ?</p>
        <div class="cont-flex-space">
            <a class="btn-type btn-form btn-alert btn-danger"
                href="{{path('delete_contract', {'contract': contract.id})}}">Supprimer</a>
            <a class="mdl-close-btn btn-type btn-form btn-cancel">Annuler</a>
        </div>
    </div>
</div>
{% endfor %}
{% endfor %}